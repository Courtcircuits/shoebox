apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ldap-server
  namespace: argo
spec:
  project: applications
  source:
    repoURL: "https://github.com/Courtcircuits/shoebox"
    path: ldap
    targetRevision: HEAD
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: "https://kubernetes.default.svc"
    namespace: ldap
  syncPolicy:
    automated:
      prune: true
      selfHeal: true

